timesheet-tracker需求更新，未被说明的功能就还是原来的不变：
1，去掉底下今日待办和一周待办tab和列表，已完成tab默认显示区域全部收起，点了时候才弹起来，还是可以上下拖拽壳显示区域，目的是为了把视图留更多给任务视图
2，底下的待办列表去掉后，每日填报工时以及所有和一周任务所有操作都放到任务视图中操作，甘特任务条上的任务名称和总工时放到任务条的左边外侧区域，任务条上按每天划分等宽区域可以直接填写每日工时，双击某个日期对应任务条，修改工时数，回车或blur变回可读态，任务条右侧外面放操作类，修改任务，删除任务，复制任务，都用小图标hover上去显示名称，点击复制任务图标在今日创建一个副本任务，删除任务需要二次确认，修改任务打开任务编辑弹窗
3，这样修改后会出现如果一个任务是从周一开始，或者周五结束的任务，任务条外侧会没有空间展示，那么把每周任务视图的开始日期从周天开始到周六结束，这样可能会效果好点，任务名显示不下，就省略号显示； 日历日期表头点击时弹窗可以填报/批量修改当天所有任务工时数，没有默认时间和最大数限制，用户可以填报历史日期工时，可以批量填报某天所有任务工时，
如果以上有不确定的逻辑可以先和我确认再开始写代码

1，不是，因为现在一行就是一个任务，如果任务在周三一天，那么整个视图的这行任务条左右侧都还有空白位置，可以用来展示任务名称和操作图标，但是这样也是有点问题，比如周一开始的任务左侧就没有多少空余空间了，这个我们可以先做一版看看效果，不行再用你的方案加一列固定标签区；
2，只读态要显示已填工时数，双击后改为编辑态才显示输入框
3，日期表头弹窗点开后是显示在这天所有任务，已填的任务工时数要显示，没有8h上限，可以填0清空该任务工时数
4，你理解的是对的，已完成不要后面的任务数，因为这里是所有历史已完成任务，点击后展开列表，有点过渡动画
5，是的

待确认：
1，任务

补充需求说明：
1，不可crud上上月前所有历史数据，因为已经归档了，不可再有任务操作
2，

4，任务视图每行展示调整，如果这条任务是6.16周二开始到6.17周三结束，那么任务名称的展示应该在周一这天列位置上，任务名称在任务条开始日期的前一天，贴紧，这样不会出现任务名称在在最左列，但是任务是周五的，离的很远，当这个周期的任务有周天开始时往前再新增一列空白区域展示任务名称；任务条后面的操作图标也不要放在最后面，同理，应该是放在任务结束的后一天区域；
5，点击任务条不要打开编辑弹窗，只有在点击编辑按钮时才打开，编辑弹窗要加上每天具体的工时数和任务总工时数显示，并且能修改单个工时数，日期表头能看到当天汇总的已填工时数，弹窗打开也是可以看到；任务条上的工时数双击时变成编辑态输入框，能在不打开弹窗的情况下直接改这一个数字
6，不可crud上上月前所有历史数据，因为已经归档了，不可再有任务操作


1，现在的任务条展示效果不好，你现在把任务名称就放在任务条的左边前一天列，调整为动态前一列定位，任务操作图标动态在右边后一天列，
2，只有在空白行操作才新增任务，打开新增弹窗，
3，现在是6月，页面上还是可以在4月新增任务，要修复下
4，新建一个任务时，弹窗列出起止时间区间的每天工时数为0，当用户把编辑任务弹窗打开时，也可以继续填工时，不是现在创建时没有显示工时数
任务视图和团队热力图的列宽都改为有最小宽度，然后可以自由随着父容器自动计算宽度

OK，现在功能基本上已经是我想要的了，现在全面整理下代码，把不需要的类名样式方法变量通通删掉，硬编码的地方都要改为数据驱动，先把这个改了我们再开始下一步

上午和后段对接了下接口，目前设计是所有数据类的相关的数据全部改为接口请求，前端本地不存数据了